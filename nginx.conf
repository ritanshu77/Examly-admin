server {
    listen 80;
    server_name localhost;

    # Serve the React application
    # The app is built with base: '/admin', so we serve it under /admin
    location /admin {
        alias /usr/share/nginx/html/admin;
        try_files $uri $uri/ /admin/index.html;
    }

    # Proxy API requests to the backend
    # Frontend calls /api/admin/..., Backend expects /admin/...
    
    # Use Docker's internal DNS resolver to handle dynamic upstream resolution
    resolver 127.0.0.11 valid=30s;

    location /api/ {
        rewrite ^/api/(.*) /$1 break;
        set $backend_upstream http://backend:3001;
        proxy_pass $backend_upstream;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Redirect root to /admin (optional)
    location = / {
        return 301 /admin;
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
